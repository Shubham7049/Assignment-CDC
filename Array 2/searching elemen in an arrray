

int SearchElement(vector <int> & nums,int target)
{
   for(int i=0;i<nums.size();i++)
   {
      if(arr[i]==target)
      {
         return i;
         }

    }
    return -1;   //element not found
    }
